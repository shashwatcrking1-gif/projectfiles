/**
 * GridFloor — CSS-only perspective grid that recedes into the distance.
 * Purely decorative, no interactivity, aria-hidden.
 */
export function GridFloor() {
  return <div className="grid-floor" aria-hidden="true" />;
}
